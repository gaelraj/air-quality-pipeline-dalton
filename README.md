# Air Quality Pipeline

This project collects air quality data from OpenWeather, stores raw JSON files, builds a clean CSV dataset, validates the data, loads it into a PostgreSQL warehouse, and orchestrates the workflow with Apache Airflow.
